const initialState = {
    whiteNav: false,
    headerTrans: false
};

export default function(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
};